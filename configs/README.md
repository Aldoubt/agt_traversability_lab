# Configuration Files

This directory stores experiment parameters.

Planned configuration:

- MID360 sensor parameters
- ROS2 topic configuration
- Frame definitions
- Algorithm parameters

Configuration files should be version controlled together with experiment results.
